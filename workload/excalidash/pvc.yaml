apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: excalidash-backend
  namespace: excalidash
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
